# input which allowed user rating an expert based on a package service 

input CreatePackageInput {
    package_name: String!   #name which resume is based upon
    turn_around_time: TurnAroundTime!
    amount: Float!
    form_type:FormType!
}

input UpdatePackageInput {
    id: ID!
    package_name: String!   #name which resume is based upon
    turn_around_time: TurnAroundTime!
    amount: Float!
}
